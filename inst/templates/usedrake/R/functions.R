# Your custom code is a bunch of functions.
# Scripts like these *prepare* to do the work,
# they do not actually build any targets.
# More information:
# https://ropenscilabs.github.io/drake-manual/projects.html#script-file-pitfalls

# create_plot <- function(data) {
#   ggplot(data, aes(x = Petal.Width, fill = Species)) +
#     geom_histogram(binwidth = 0.25) +
#     theme_gray(20)
# }
